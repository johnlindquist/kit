// Import the specific functions from date-fns
import {
  formatDistanceToNow,
  compareAsc,
  format,
  parseISO,
  formatDistanceToNowStrict
} from 'date-fns';

// Re-export them for the bundle
export {
  formatDistanceToNow,
  compareAsc,
  format,
  parseISO,
  formatDistanceToNowStrict
}; 