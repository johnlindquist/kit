// Description: Templates

import {
  backToMainShortcut,
  closeShortcut,
} from "../core/utils.js"
setName(``)

await arg({
  placeholder: "Reserved for future use",
  enter: "Exit",
  shortcuts: [backToMainShortcut, closeShortcut],
})

export {}
