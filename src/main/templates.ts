// Description: Templates
// Exclude: true

import {
  escapeShortcut,
  closeShortcut,
} from "../core/utils.js"
setName(``)

await arg({
  placeholder: "Reserved for future use",
  enter: "Exit",
  shortcuts: [escapeShortcut, closeShortcut],
})

export {}
